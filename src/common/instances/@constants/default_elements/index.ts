import { Mococa } from '@types';
import { Layout } from './Layout';
import { Page } from './Page';
import { Text } from './Text';

export const default_elements: Mococa.Component[] = [Page, Text, Layout];
